import math
import webgl
import dom

class MyGL extends GLNode{

	mesh = webgl.vec3Rect(0,0,0.5,0.5)
	vertex: mesh[]
	pixel: #orange
	draw: (){
		clearAll(#aero)
		draw(this)
	}
}

// build the simplest of scenegraphcs
GLView{
	init(document.body)
	MyGL{
		
	}
}